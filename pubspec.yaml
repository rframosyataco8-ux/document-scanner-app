name: document_scanner_app
description: DocScan Pro - Escáner profesional conectado al Sistema de Guías de Cacao (RomEx).
publish_to: 'none'
version: 1.3.0+4

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_mlkit_document_scanner: ^0.5.0
  path_provider: ^2.1.4
  share_plus: ^10.0.2
  intl: ^0.19.0
  uuid: ^4.4.2
  shared_preferences: ^2.3.2
  http: ^1.2.2
  mobile_scanner: ^5.2.3
  path: ^1.9.0
  connectivity_plus: ^6.1.0
  firebase_core: ^3.8.0
  firebase_messaging: ^15.1.5
  flutter_local_notifications: ^18.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
